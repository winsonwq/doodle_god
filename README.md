# doodle_god

God created the World

## Installation

####Install grunt in global

```
npm install -g grunt-cli
```

####Install bower in global

```
npm install -g bower
```

####Download dependencies

```
npm install grunt-contrib-connect --save-dev && bower install
```

####Run the server

```
grunt connect:server
```